
export type OutputFormat = 'image/jpeg' | 'image/png' | 'image/webp';
export type TextAlign = 'left' | 'center' | 'right';

// Add other shared types for the image editor here as needed
// For example, for Color Tune:
// export interface ColorTuneSettings {
//   brightness: number; // e.g., 0-200, 100 is normal
//   contrast: number;   // e.g., 0-200, 100 is normal
//   saturation: number; // e.g., 0-200, 100 is normal
//   grayscale?: boolean;
// }
