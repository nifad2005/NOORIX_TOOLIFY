
import ImageConverter from '@/components/ImageConverter';

export default function ImageConverterPage() {
  return (
    // The main container in layout.tsx will handle padding
    <ImageConverter />
  );
}
